{
  "name": "astrelia-web",
  "version": "0.0.0",
  "private": true,
  "description": "Web workspace + Playwright headless tests for Astrelia",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
