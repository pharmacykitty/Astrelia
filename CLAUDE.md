# Astrolabe (working title)

> A point-at-the-sky planetarium for iOS with deep astrophysical data and a full astrology layer — built to be genuinely beautiful.

**Status:** Pre-prototype. This file is the source of truth for the vision and architecture until code exists. Name is not final — see "Naming" below; renaming the folder is a one-liner, don't treat `Astrolabe` as locked.

---

## The vision

Raise your phone toward the sky (or the ground, to see what's beneath you) and see the real positions of stars, planets, and other celestial bodies, accurately computed for your location and the current instant. Tap anything to get as much detail as exists — physical (mass, radius, distance, spectral class, temperature, luminosity, orbital elements…) and, in a clearly separate layer, astrological (zodiac, houses, aspects, transits, natal charts).

Three pillars:
1. **Accurate** — real ephemeris math, real catalogs. Positions should match Stellarium/SkySafari within reason.
2. **Deep** — every body carries the richest dataset we can source; tapping should always reward curiosity.
3. **Beautiful** — this is the differentiator. Smooth motion, gorgeous star glow / Milky Way, tasteful typography and transitions.

Astrophysics and astrology coexist but are **separate modules** — different math, different data, different UI surfaces. We don't blend the science and the symbolism in code; the user chooses what they want to see.

### Two viewing modes (same data engine, two cameras)
1. **Sky mode** — you're standing on Earth. Raise the phone; real bodies are projected onto the celestial dome (RA/Dec → alt/az for your location and time). This is the AR "point at the sky" experience.
2. **Galaxy Map mode** — you leave Earth and free-fly through a 3D star field, à la *Elite Dangerous*. Zoom from a stylized whole-galaxy view down to a single system; click to select and inspect.

Both consume the same `CelestialCore` data — Sky mode uses horizontal coordinates; Map mode uses the stars' true 3D positions (RA/Dec + parallax distance → XYZ).

---

## Tech stack (proposed — confirm as we go)

- **Platform:** iOS, latest SDK (Xcode 27 / Swift 6, strict concurrency). iPhone first; iPad later.
- **UI:** SwiftUI for chrome and detail views.
- **Rendering:** prototype the sky dome with SceneKit or SwiftUI `Canvas`; move to **Metal** for the real thing (100k+ stars, glow shaders, Milky Way, 60/120fps). Treat the renderer as swappable behind a protocol.
- **Sensors:** `CoreLocation` (observer lat/long/altitude + true heading) + `CoreMotion` (`CMMotionManager` device attitude, referenced to true north via `.xTrueNorthZVertical`). Sensor fusion answers "where on the celestial sphere is the device pointing right now."
- **Persistence:** SwiftData for user data (saved locations, natal charts, favorites). Bundled read-only catalogs ship as data files, not SwiftData.

## Architecture

Keep the math/data **platform-agnostic and unit-tested**, isolated from UI and sensors.

```
App/                       # SwiftUI iOS app target
  Sky/                     # ✅ sensors, AR, projection (SkyCamera/SkyMotionProvider/ARCameraController)
  Screens/                 # Catalog, Constellations, Galaxy Map, Astrology, About screens
  Catalog/                 # ✅ catalog/constellation data + facts (StarFacts, ConstellationCatalog…)
project.yml                # XcodeGen spec — the .xcodeproj is generated, not committed
Packages/
  CelestialCore/           # local Swift package — NO UIKit/SwiftUI deps
    Math/                  # ✅ Angle, Vector3
    Time/                  # ✅ Julian date, sidereal time (mean+apparent), ΔT, nutation
    Coordinates/           # ✅ equatorial ⇄ horizontal, precession (J2000→date), refraction
    Catalog/               # ✅ Star, HYG CSV ingestion, StarCatalog queries · ✅ DeepSky (Messier+NGC)
    Ephemeris/             # ✅ Sun & Moon (Meeus 25/47, Moon topocentric) · ✅ Planets Mercury–Pluto (SwiftAA, MIT) · ✅ AstroEvents
  Astrology/               # ✅ zodiac, ayanamsa, angles, houses, aspects, charts (separate package)
```

- `CelestialCore` is pure computation: feed it a time + observer + body, get back coordinates and physical data. Fully testable against published reference values (Meeus worked examples, JPL Horizons).
- The app layer turns `CelestialCore` output into the rendered dome and detail UI.
- `Astrology` depends on `CelestialCore` (it needs ecliptic longitudes) but nothing depends on it.

### Hard problems to get right
- **Coordinate transforms & time:** Julian date → local sidereal time → equatorial (RA/Dec) → horizontal (alt/az) for the observer. This is the spine; build and test it first.
- **Ephemeris accuracy vs. weight:** analytical models (VSOP87 for planets, ELP for Moon) vs. bundling JPL DE ephemerides. Start analytical; revisit if accuracy demands it.
- **Sensor fusion & smoothing:** magnetometer is noisy; needs filtering/calibration so the sky doesn't jitter. Account for magnetic vs. true north.
- **Rendering performance & beauty:** culling, level-of-detail by magnitude, additive glow, all at 60fps.

### Galaxy Map mode (the *Elite Dangerous*-style free-flight map)
> **How it's built — read this for the Galaxy Map:** the full writeup of the renderer,
> star field, Milky Way, landmarks (procedural + image-baked), depth occlusion, octree
> culling, camera/free-fly, the on-screen menu, and exoplanets lives
> in **`docs/galaxy-map.md`**. Keep it in sync when changing the map.

The trick is separating **real data** from **art**:
- **Real (near field):** the ~119k catalogued stars from HYG have true 3D positions (parallax → XYZ in parsecs). Render them as glowing billboards in a free-fly 3D scene; click to select and inspect. Fly to a star and show its **actual known exoplanets**.
- **Art (far field):** we will never have positions for 100B+ stars. Render the Milky Way's spiral arms as a stylized volumetric/particle backdrop, and overlay the real catalog where we have it. Zoom: stylized whole-galaxy → real local stars → single system.
- **Not in scope:** Elite's literal procedural 400-billion-system simulation. We get the visual magic without it.
- **Engineering caveats:** this is a *second* rendering pipeline (free-fly camera, Metal). Huge scale ranges demand floating-point precision care (camera-relative / scaled coordinate space, logarithmic depth). Needs a spatial index (octree/k-d tree) for picking and culling. Shares `CelestialCore` data; separate renderer.

## Data sources (verify licensing before bundling — this matters for App Store)
> The user-facing, canonical attribution list lives in `App/Screens/AboutView.swift` (`SourceCatalog`), surfaced as the **About → Sources** screen. Keep it in sync with this section.
- **Stars:** HYG Database v3 (~119k stars, combines Hipparcos/Yale/Gliese; public domain) is the likely starting catalog. Yale Bright Star Catalog for the brightest.
- **Deep-sky:** Messier / NGC catalogs.
- **Exoplanets:** NASA Exoplanet Archive (~5,700+ confirmed planets with host-star links) — powers "fly to a star, see its planets" in Galaxy Map mode. Full design + decisions in **`docs/exoplanet-spec.md`**.
- **Planets/Sun/Moon:** VSOP87 / ELP analytical theories, or JPL data. Physical/astrophysical facts from NASA/JPL fact sheets.
- **Derived stellar physics:** surface temperature from B−V via Ballesteros (2012); radius from luminosity + temperature via the Stefan–Boltzmann law (IAU 2015 nominal solar values). Lives in `CelestialCore.Astrophysics` (pure + unit-tested); relatable comparisons in `App/Catalog/StarFacts.swift`.
- **Candidate library:** `SwiftAA` (Swift port of Meeus' *Astronomical Algorithms*) — could save large effort on Time/Coordinates/Ephemeris. **Check license** and accuracy before depending on it.
- **Astrology:** house systems (Placidus, Whole Sign, etc.), aspect math. Swiss Ephemeris is the gold standard but is **AGPL or paid-commercial** — incompatible with a closed App Store app unless licensed. **Settled (2026-06-27):** roll our own astrology math on `CelestialCore`; use **SwiftAA (MIT)** for planet ecliptic longitudes. Full spec + decisions in **`docs/astrology-spec.md`**.

## Conventions
- Swift 6, strict concurrency. Keep `CelestialCore` `Sendable` and free of global mutable state.
- Astronomy math in SI / standard units with explicit types where it prevents unit bugs (radians vs degrees, JD vs calendar date). No naked `Double` for angles in public APIs where avoidable.
- Unit tests are mandatory for `CelestialCore` — every transform validated against a known reference value.
- Separation of concerns per the existing architecture-review/swiftui-pro skill standards: no sensor or UI code inside the math packages.
- **No emojis anywhere in the UI.** Use proper Unicode **text-presentation** symbols (astrological/astronomical glyphs, SF Symbols). Several glyph codepoints — notably the zodiac signs (U+2648…U+2653) and ♀/♂ — default to *emoji* presentation on iOS; append the text variation selector **U+FE0E** (`︎`) to force the line-art glyph. Don't hardcode glyph literals: reuse `ZodiacSign.glyph` / `AstroBody.glyph` (which already carry FE0E where needed).
- **Keep sources current.** After adding any dataset, library, algorithm, or doing research that informs the app, update the attributions: add an entry to `SourceCatalog` in `App/Screens/AboutView.swift` (the in-app Sources screen — the source of truth users see) and, where relevant, the "Data sources" section here and the per-feature spec's Sources section in `docs/`. Several datasets require attribution before shipping, so this is not optional.

## Roadmap
- **Phase 0 — now:** vision + this doc + name. Decide on `SwiftAA` vs. roll-our-own.
- **Phase 1 ✅:** `CelestialCore` Time + Coordinates — `Angle`, `JulianDay`, mean sidereal time, equatorial⇄horizontal transforms. Validated against Meeus worked examples (7.a, 12.a/b, 13.b). Conventions locked: azimuth from **North eastward**, longitude **east-positive**. (Decision: rolled our own; revisit SwiftAA at Phase 2 for ephemeris.) **Apparent-place accuracy ✅:** nutation (Δψ/Δε, Meeus 22), true obliquity, apparent sidereal time (equation of the equinoxes), ΔT (Espenak–Meeus UT↔TD), and rigorous precession (Meeus 21, J2000→date) — all unit-tested against Meeus worked examples. Atmospheric **refraction** (Bennett) added too.
- **Phase 2 (in progress):** Star catalog ingestion ✅ (HYG parser + `StarCatalog`, validated on full ~120k-row HYG v4.1). Sun & Moon ephemeris ✅ — rolled our own: Sun (Meeus 25, apparent ~0.01°), Moon (Meeus 47, full 60+60-term ELP series, validated to Meeus 47.a), obliquity, ecliptic→equatorial transform, Moon phase/illumination. iOS app shows live Sun/Moon/Sirius alt-az + Moon phase. **Decision recorded: kept rolling our own — SwiftAA not needed so far.** Nutation/apparent-sidereal/ΔT ✅ (see Phase 1) and planets ✅ (SwiftAA in `CelestialCore.Planets`, used by charts). **Moon topocentric parallax ✅** (Meeus 40) for the observer's location.
- **Phase 3 (first cut ✅):** Sensor fusion — `SkyMotionProvider` (CoreLocation + CoreMotion `.xTrueNorthZVertical`), `CameraBasis`/`projectToScreen` gnomonic projection mapping device pointing → screen. Center reticle + heading HUD. ⚠️ Rotation-matrix row/column convention is a guess until verified on a real device.
- **Phase 4 (✅ working):** Live AR "point at the sky" view, verified on-device. Full naked-eye star catalog (HYG mag ≤ 6.5, ~8.9k stars, bundled as `App/Resources/hyg_naked_eye.csv`, ~560 KB) drawn via `Canvas` (sized/coloured by magnitude & B−V), brightest stars labelled, Sun & Moon as nodes. Filter sheet (brightness/labels/constellations/Sun-Moon/below-horizon). Bottom panel guides to Sun & Moon only. **Constellation lines** (d3-celestial GeoJSON, BSD, bundled `constellation_lines.json`). **Pinch-to-zoom** FOV reveals more star names; **collision-avoided labels** (proper name → Bayer); **tap-to-identify** any star.
- **Phase 3b/4b — AR camera mode (first cut, needs on-device verification):** `SkyCamera` abstraction unifies projection for two modes: **Sky** (CoreMotion gnomonic) and **AR** (`ARCameraController` = ARKit `.gravityAndHeading` camera passthrough, projected via real `viewMatrix`/`projectionMatrix`). Mode toggle in chrome. **Calibration**: aim reticle at Sun/Moon/bright star → "Align" computes an azimuth offset cancelling compass error (→ sub-degree). Precision ceiling = magnetometer (~5–10° raw, <1° after align); altitude from gravity ~1–2°. Ultimate future precision = camera plate-solving. Next: planets, full-name constellation labels, perf (cache star dirs past mag ~5.5). Attribution before ship: d3-celestial (Frohn, BSD), HYG (astronexus).
- **Phase 5 (largely ✅):** Tap-to-detail astrophysical data sheets, with an accessibility-first "numbers → intuition" layer. Done: `CelestialCore.Astrophysics` (derived stellar radius via Stefan–Boltzmann, light-travel time, parsec↔ly; unit-tested) + `StarFacts` "relatable comparisons" (light left in year Y, ×Sun brightness/heat/size) and `lifeStory` (one-line stellar biography from spectral/luminosity class). Catalog `StarDetailView` now carries a derived-radius row, comparison callouts, the life story, an **HR-diagram "you are here"** widget (`StarComparisonViews.swift`), and a true-scale **Sun-vs-star size silhouette**. Galaxy Map shows a persistent **distance-from-Earth** readout and a light-travel line on the star card. Sky mode: tap-identify surfaces distance + plain-language spectral type, a **colour-key (temperature)** legend toggle, and an opt-in **time scrubber** (±12h, runs the Sun/Moon/stars forward/back on the live ephemeris). **Planet/exoplanet detail sheets ✅:** `CelestialCore.Astrophysics` adds surface gravity + K→°C; `PlanetFacts` relatable comparisons (size vs Earth/Jupiter, your weight there, °C with feel, year length, habitable zone, starlight age, discovery); `PlanetDetailView` opened via "Full details" from `SystemView`. **Tonight's Sky feed ✅:** `CelestialCore.VisibleSky` + `RiseSet` (altitude-scanning rise/set, unit-tested) compute the Moon phase, day/night with sunrise/sunset, and naked-eye planets up-now/below-horizon for the observer; `TonightView` (+ `ObserverLocation`) is the first Explore menu entry. **Meteor showers ✅** (`CelestialCore.MeteorShowers`, IMO working list) and **astronomical events ✅** (`CelestialCore.AstroEvents` — solstices/equinoxes, Moon phases, oppositions/conjunctions, greatest elongations, perigee/apogee, perihelion/aphelion, approximate eclipse flags; coarse-to-fine search on the live ephemeris, unit-tested) both surface as Tonight sections. **Jargon glossary ✅** (`Glossary` + `GlossaryButton` popovers, A–Z browser from About). **Deep-sky catalog ✅:** `CelestialCore.DeepSky` (Messier 110 + famous NGC) browsable in the Catalog with detail pages. **Sky-mode accuracy ✅:** the Moon now uses topocentric parallax and the luminaries apply atmospheric refraction. **Next:** deep-sky objects pointable/identifiable in Sky mode; full-sky refraction toggle; ISS passes (needs TLE).
- **Phase 6 (first cut ✅ — math layer):** `Packages/Astrology/` built and unit-tested (22 tests, `swift test` green). Done: zodiac signs/positions + DMS; `Zodiac` tropical/sidereal with `Ayanamsa` (Lahiri); `ChartAngles` (Ascendant/MC); `HouseSystem` = Whole Sign / Equal / Porphyry / **Placidus** (iterative semi-arc, validated against its own definition) with **high-latitude fallback to Porphyry**; `AspectKind`/`OrbPolicy`/`AspectFinder` (major+minor, luminary bonus, applying/separating); `EphemerisProvider` + `CelestialCoreEphemeris` (Sun, Moon, mean lunar nodes today); `NatalChart` (positions, houses, aspects, Part of Fortune, day/night sect). App: `AstrologyView` ("Sky Now" chart — Big Three, `ChartWheel` Canvas, positions/aspects), wired into the menu. **Planets ✅ (Phase B done):** `CelestialCore.Planets` computes apparent geocentric ecliptic longitudes for Mercury–Pluto via **SwiftAA (MIT)** (Pluto via Meeus 37 + Earth, precessed to date); validated against Meeus 33.a (Venus RA/Dec), inferior-planet elongation bounds, and published Pluto ephemeris. Charts now carry the full body set + retrograde. Full spec/decisions in **`docs/astrology-spec.md`**; features in **`docs/astrology-features.md`**. **Birth charts ✅:** `SavedChart` (SwiftData, stores raw inputs incl. IANA tz) + `ChartEditorView` (date/time, "time unknown", `CLGeocoder` place→coords+timezone, house/zodiac pickers) + `ChartListView` + `SavedChartView`. Astrology menu → `AstrologyHomeView` hub (Sky Now / Saved Charts). Shared `ChartDetailView` (Big Three, `ChartWheel`, positions, aspects). **Transits ✅:** `Astrology.Transits` (cross-aspects transiting→natal) + `TransitsView` (linked from saved charts). **Interpretation ✅:** `Astrology.Interpretation` — owned, templated keyword engine (planet-in-sign/house, aspect, transit), deterministic + no licensing; surfaced by tapping rows in `ChartDetailView` (→ `ReadingSheet`). **3D celestial sphere ✅:** `CelestialSphereView` — rotating Astrolog-style luminous globe in the local horizon frame: real HYG **star field** (spectral-coloured, diffraction spikes), faint **constellation** figures, glowing ecliptic + zodiac, graduated horizon + cardinals, **house great-circles**, colour-coded glowing planets with **label de-collision**, aspect chords. The ★ astrophysics×astrology bridge. Design notes in **`docs/celestial-sphere.md`**. (Debug-only `-snapshotSphere` launch arg → `SphereSnapshotHarness` for capturing it.) **Engine deepening ✅:** dignities + chart ruler (`Dignities`), chart-shape patterns (`Patterns`: stellium/grand trine/T-square/grand cross/yod), extra bodies (`CelestialCore.MinorBodies` — Chiron + Ceres/Pallas/Juno/Vesta via two-body Kepler from JPL SBDB elements, Black Moon Lilith; validated <0.5° vs Horizons; opt-in via `AstroBody.standard` vs `.points`), sectioned interpretation (`Interpretation.planetReading` → in-sign/in-house/dignity/aspects), transit **forecast** + retrograde stations (`Forecast`, bisection-exact dates) with local-notification alerts (`TransitNotifications`), and **daily readings** (`Daily`). **Relationship astrology ✅:** `Synastry` (cross-aspects + 0–100 compatibility score) + `Composite` (midpoint chart); UI `RelationshipsView` (pick two saved charts) → `SynastryView` (a `SynastryBiWheel` + score + composite toggle), reached from the Astrology hub's **Relationships** card and a **Compare** button on Saved Charts. **Chart UI deepening ✅:** `ChartDetailView` gained pattern chips, a chart-ruler card, dignity badges, a points/asteroids drawer, sectioned `ReadingSheet`; `AstrologyHomeView` gained a "Today" daily-reading hero with chart picker; `TransitsView` gained Now/Upcoming modes with a retrograde banner. **Export/share ✅ (F13):** `SphereExport` renders a full ~10 s sphere rotation to **GIF** (ImageIO, fewer/smaller frames) and **MP4** (AVAssetWriter, full-res) from the sphere's toolbar — paced per-format via `SphereExportFormat.renderPlan`; shared through `ShareSheet`. (Live Photo was built then **cut** — a static-looking 10 s loop wasn't worth the metadata complexity.) `ChartShareCard` renders three shareable poster styles (Wheel / Big Three / Full readout) via `ImageRenderer` from `ChartDetailView` (debug `-exportTest` arg renders all to Documents for verification). **AR zodiac overlay ✅ (F11, on-device verified):** a Sky-filter toggle ("Ecliptic & zodiac") draws the ecliptic, the 12 sign glyphs, and the live planets at their true sky positions (reuses the star projection pipeline; computed on the calm refresh). **Sphere legibility + depth ✅:** a **"What am I seeing?"** guide (`SphereGuideSheet`) — a colour-keyed legend of every element + a tappable placements list (→ sectioned reading); **tap-to-identify** on the globe (frozen-yaw model: tap a planet to freeze + read it via a selection card/ring, tap empty space to pause/resume); planets drawn at their **true ecliptic latitude** (`SphereGeometry.eclipticLatitude`, ayanamsa-invariant); and a soft **Milky Way band** (`galacticToEquatorial` → galactic equator glow). **Predictive astrology ✅:** secondary **progressions** (`Astrology.Progressions` — "a day for a year": progressed positions read against the natal houses, progressed→natal cross-aspects, solar arc + solar-arc-directed angles) and **returns** (`Astrology.Returns` — solar-return year-ahead + lunar-return month-ahead charts, found by tropical-longitude bisection; `Returns.upcoming` for a run); both surfaced in `PredictiveView` (Progressions / Solar Return / Lunar Return segments, linked from `ChartDetailView` beside Transits), with overview/placement/aspect copy in `Interpretation` (`progressionsOverview`/`progressedPlacement`/`progressedAspect`/`returnOverview`). Unit-tested (`PredictiveTests`: day-for-a-year mapping, ~1°/yr solar arc, solar return perfects the Sun & lands on the birthday, lunar-return cadence). **Richer reading copy ✅** (fuller two-sentence sign/house/aspect/transit interpretations). **Sphere comprehension & motion pass ✅:** **house numerals** (Roman I–XII at each house's mid-longitude); **aspect storytelling** (chords coloured by harmony, the tightest aspect gently pulses, tap a chord for its reading, selecting a planet dims the aspects it isn't in — `hitTestAspect`); a **guided tour** (menu → Guided tour) that auto-spotlights Sun → Moon → Rising → tightest aspect with one-line captions; and **time travel** (menu → Time travel) — a ±24 h scrubber + play that rebuilds the sphere at `chart` + offset (`effectiveChart`) so the whole sky wheels in true diurnal motion while the camera holds (live readout + Now reset). Debug `-snapshotSphere [tour|time]` arg drives the overlays for screenshots. **Deeper interpretation ✅:** retrograde-aware `planetReading` (a Retrograde section), **house-ruler** readings (`Interpretation.houseRuler` + a House-rulers card in `ChartDetailView`), and a holistic **elemental/modal balance** read (`Interpretation.elementalBalance` + an Elements & Balance card). **Sphere polish ✅:** the **guided tour auto-starts after ~5 s idle** (cancels on any drag/tap, tracked via `interactionTick`), and **depth occlusion** — the additive backdrop (Milky Way, constellations, stars) is drawn in a far pass → translucent globe-body veil → near pass, so the near hemisphere occludes the far one instead of far-side light shining through. **Ship-readiness (in progress):** About→Sources gained IAU constellations, cultural sky figures, and SIMBAD/CDS (landmark positions); glyph fix — `AstroBody.glyph` and `ContentView.planetSymbol` now append U+FE0E so ♀/♂ never render as colour emoji; privacy strings present. **Open:** a differentiated App Store name (Astrolabe is taken). **Next:** finalize name; iPad/accessibility deferred (no iPad to test).
- **Phase 7 (first cut ✅):** **Galaxy Map mode** *(full build writeup: **`docs/galaxy-map.md`**)* — `GalaxyMapView` renders the real local star field in 3D: HYG positions computed from RA/Dec/parallax → XYZ parsecs, Sun at origin. SwiftUI `Canvas` + a simd orbit camera (`lookAt`/`perspective` matrices); drag to orbit, pinch to zoom, tap-to-select (projected-nearest hit test), "Fly here" retargets the camera. Stars sized by absolute magnitude, coloured by B−V. Wired into the menu. **Now also:** larger catalog (bundled `hyg_stars.csv`, mag ≤ 7.5, ~25k distance-known stars with real HYG XYZ/absmag/spect); stylized **Milky Way** as a true-scale four-arm barred spiral centred on the galactic centre (~8.2 kpc out) with additive/blurred glow, HII knots and a bar; **Sgr A\*** + a curated **Landmarks** dataset (`App/Catalog/Landmarks.swift`: ~25 nebulae/clusters/black holes/satellite galaxies) rendered as tappable markers; a searchable **Catalog** screen (`App/Catalog/CatalogView.swift`) of landmarks + bright stars with detail pages that launch the map focused on an object (`GalaxyMapFocus`); **free-fly camera** (Fly toggle) alongside orbit — gesture-only controls: one-finger drag steers around a centre reticle, a window-level **two-finger vertical pan** sets a *persistent* throttle (cruise hands-free; squared speed curve for fine→galaxy-crossing range, zero detent), with a non-interactive speed readout; plus a whole-galaxy "Galaxy" overview and eased fly-to. Full-screen (ignores safe area), custom back button. **Now also:** **exoplanet layer** — NASA Exoplanet Archive snapshot (`App/Resources/exoplanets.csv`, ~6.3k planets / ~4.7k systems incl. `sy_snum`) loaded by `ExoplanetStore`, cross-matched to catalog stars by HIP; host stars get cyan badges on the map and a "View planetary system" button (below "Fly here", shown only when the selected star has displayable planets). `SystemView` is a top-down orbital view: host star(s) at centre (companions clustered for multiple-star systems), planets on log-AU orbits with always-on name labels and Keplerian relative speeds, habitable zone, tap-to-inspect; a hand-authored **Sol** (`SolarSystem` in `Exoplanets.swift`) adds the planets' **major moons** (archive has no exomoons), drawn orbiting a selected planet. Galaxy-map **Catalog button** + a **Milky Way toggle**; button taps take priority over star selection (gestures live on the Canvas layer below the overlay). **Beauty pass ✅:** landmarks render as genuine **volumetric** 3D objects (gas/dust/stars distributed in world space and projected individually, so they parallax/rotate and you can fly into them) — `drawNebula` (clumpy emission clouds, filamentary SNR shells, bipolar planetaries, two-tone H-alpha/OIII, embedded stars), `drawSatelliteGalaxy` (tilted disc + bar + warm core for LMC/SMC), `drawBlackHole` (tilted accretion disc + photon ring + Doppler beaming for Sgr A\*/Cyg X-1). **Dust lanes** via a normal-blend dark pass that subtracts light — threading the Milky Way's arms and carving the dustiest nebulae (Trifid's three lanes, Eagle's pillars; Horsehead rendered as a dark pillar silhouetted on emission). Milky Way overhauled for grandeur: warm-core→cool-rim colour gradient, arm spurs, stellar halo, layered radiant core, **two-pass additive bloom** (heavy-blur haze following the real arm/bar structure + crisp cores), 8-level opacity (de-banded), plus **adaptive LOD** (stride-subsample with opacity compensation when the galaxy is small on screen). **Spatial index ✅:** `CelestialCore.PointOctree` (pure, `Sendable`, unit-tested vs. brute force) — built once over the star positions and queried each frame with the view-frustum planes (`frustumPlanes`, Gribb–Hartmann; four sides + a behind-camera plane) so the renderer and the tap-picker only touch in-view stars instead of all ~25k. Conservative (whole in-view subtrees added test-free; straddling leaves over-included, then the existing screen-bounds/distance cull refines); brightness-rank ordering preserved by sorting the visible set only when it would hit the LOD cap. **Metal renderer (in progress — behind a beta toggle):** `App/Screens/Galaxy/` — one instanced billboarded **soft-sprite** pipeline (`GalaxyShaders.metal`) renders the whole scene as additive light sprites (stars, Milky Way, gas, glows) + normal-blend dark sprites (dust, event horizons); `GalaxyMetalRenderer` (`MTKView` + `UIViewRepresentable`) with **camera-relative coordinates** (eye at origin → float precision) and **logarithmic depth**. Instances are built once in world space from the same catalogue/art math (`buildScene` in `GalaxyMapView`); the GPU does all projection, so it scales to 100k+. **Milestone 1 done:** star field (+ bright-star glow) and the Milky Way (crisp + bloom + dust) render via Metal (verified on-device). **Milestone 2 done:** every remaining layer ported — `appendLandmarkSprites` emits world-space sprites for all landmark types (volumetric nebulae incl. Trifid/Eagle dust lanes + the Horsehead dark pillar, SNR shells, planetary nebulae, open/globular clusters, satellite galaxies with disc+bar+core, and **3D tilted accretion discs** for black holes), plus the Sun, a modest nucleus glow, and `hostsOnly` filtering; labels, the Sun caption and the selection ring render as a lightweight vector Canvas (`metalAnnotations`) over the Metal layer. Toggle "Metal renderer (beta)" in the map's Options. Picking/culling still run CPU-side via the octree (unchanged). **Milestone 3 ✅ — Metal is now the only renderer:** the SwiftUI `Canvas` draw path (`draw`/`drawLandmark`/`drawNebula`/`drawSatelliteGalaxy`/`drawBlackHole`, the star/backdrop loops, the beta toggle, `backdropOpacityLevels`) is **deleted**; the Galaxy Map renders entirely through `GalaxyMetalView` + `metalAnnotations`. Phase 7 complete. (Picking/culling still CPU-side via the octree; `makeViewProjection`/`project`/`frustumPlanes` retained for that and the annotations.) **Accuracy note:** landmark *positions, distances, physical sizes and types are real*; procedural morphology is a stylized archetype. **Image-baked nebulae (true shape + true colour) ✅:** `tools/nebula_bake.py` turns a **visible-light** public-domain / CC BY 4.0 photo into a particle dataset (`.nbl`: gas x,y,rgb + dust x,y, normalised) by blurring → density/colour-weighted rejection-sampling (brightness^γ, sub-pixel jitter); **the photo is never bundled, only the derived points** (copyright-clean for the App Store). `NebulaLibrary` loads the bundled `.nbl` (`App/Resources/Nebulae/`); `appendBakedNebula` orients the sheet **facing Earth** at the landmark's real position/scale and synthesises per-particle depth (front-on = the photo's shape; orbit = volumetric) — feeding the same Metal sprite pipeline. **27 nebulae baked** (`NebulaLibrary.baked`) from visible-light **public-domain / CC BY 4.0** images (ESA/Hubble, ESO, NASA/STScI, NOIRLab — all credited in `SourceCatalog`): Orion, Eagle/Pillars, Lagoon, Trifid, Carina, Tarantula, Crab, Veil, Ring, Helix, Cone, North America, Bubble, Omega/M17, Lobster, **Horsehead** (dark silhouette via particle-absence), Dumbbell/M27, Little Dumbbell/M76, Southern Ring, Butterfly/NGC 6302, Saturn, Eskimo, Vela, Jellyfish/IC 443, **Pacman/NGC 281**, Rosette, California. Most sourced by parallel sub-agents (research+fetch+verify+bake). Procedural holdouts (no good visible-light PD/CC source — all wide, faint, or IR-only): Flame (an IR object, optically behind dust), Crescent, Elephant's Trunk, Owl/M97, Cas A (optical too star-dominated), Simeis 147. **Lessons baked in:** (1) source **visible-light, not Spitzer/WISE infrared** — IR composites give "off" false colours; (2) **no dust overlay** on baked nebulae — dark lanes come for free from gas-density absence; an overlay punches black holes; (3) dense (~55–60k particles, γ≈1.4) + large soft low-opacity sprites = gas-like and gap-free without core blowout. Tool + format in `tools/README.md`. **Depth occlusion ✅:** dense, opaque landmark cores now write a **depth buffer** so background stars / Milky-Way behind a cloud are culled instead of shining through. The renderer carries a `.depth32Float` attachment on every pass; the scene splits into four sprite groups — **occluder** (invisible `sprite_occluder` caps that write depth only where a landmark is genuinely opaque: baked-nebula bright particles subsampled to trace the true shape, procedural emission lobes, globular cores, galaxy bulges), **occludee** (stars + Milky Way: `.lessEqual` depth-*test* but **no write**, so they still never occlude each other and the loved additive accumulation-glow is intact), **landmarkLight** (all nebula/landmark light — drawn depth-test-free, so a nebula never self-occludes / dims), and **overlay** (dust / horizons). Caps follow the *bright* shape, so genuinely dark gaps (e.g. Pacman's mouth) get none → stars correctly show through them; foreground stars (M42 is 412 pc) still pass since they're nearer than the caps. **Sgr A\* lensing + dive easter egg ✅ (2026-07-02, sim-verified):** `GalaxyLensing.metal` post-pass renders the galactic-centre black hole for real — Schwarzschild geodesic march (shadow, photon ring, Doppler disc) + weak-field deflection everywhere else, Einstein-lensing the actual rendered galaxy (offscreen scene + baked equirect fallback); free-flying across ~12 rs triggers a ~30 s renderer-owned plunge — a true 11.5→7 rs fall with accelerating roll, widening FOV, ~5× time-warped disc swirl, inverse aberration, a photon-ring flare + aperture collapse at the crossing, tidal stretch, white flash → eject + epilogue with a numbers→intuition HUD; reduced-res internal lens target + blit for perf. Full writeup **`docs/black-hole-dive.md`**; NASA SVS "Beyond the Brink" credited in `SourceCatalog`. On-device verify pending. **Follow-ups:** "artist's impression" tag for procedural landmarks, host-badge rings, distance-faded core, GPU-side cull/LOD; plus a **`--shape sphere` bake mode** (map the image onto a sphere so shell objects — planetaries, SNRs, Ring/Helix — read 3D from any orbit angle, not just head-on) and a star-suppression pre-pass for Milky-Way SNR bakes (Vela/IC 443 pick up foreground stars as false gas).
- **Phase 9 (first cut ✅) — Constellations & sky-figure browser:** `ConstellationsView` browses four collections via a segmented control — the official **88 IAU constellations**, famous **asterisms**, **historical** (dropped) figures, and **cultural** skies (collapsible per-tradition). Data: `ConstellationCatalog` (authored lore/family/genitive for the 88) + `SkyFigures.swift` (`FigureCatalog`/`SkyFigure`/`FigureSpec`; `StarResolver` snaps star-token figures onto live catalog stars; `SkyFigureLibrary.build` merges authored metadata with the bundled d3-celestial line geometry). Each row shows a live `ConstellationFigure` thumbnail; the detail page renders the figure large over its real catalog stars (region-pulled via `SkyRegion`, sized by magnitude / coloured by B−V through `StarColor`), using a `StereographicProjection` (east-left, north-up sky-chart convention), with curated facts (brightest star, star count) computed live. **3D constellations ✅ (`Constellation3DView.swift`):** the ★ "constellations are a line-of-sight illusion" feature — `Constellation3DModel.build` places each figure's stars at their **true HYG distances** (real XYZ parsecs, Earth at origin; figure lines snapped onto the actual star dots) and an orbit camera (own `lookAtSafe`/`perspective`, pole-safe) lets you rotate the shape so the flat pattern visibly comes apart with depth. **Earth marker** at the origin (the vantage the pattern is drawn from); fading **Earth→star sightlines** that collapse at the front view and fan into a starburst as you rotate; two-line **distance labels** (name + real ly) on the bright stars; a one-shot **depth-bloom** intro that arcs off-front and eases back to hint at the drag; **auto-rotate** toggle; and a **Front view** button that eases back to the Earth-on pose. Launched from each figure's detail page ("View in 3D"). **Next:** proper-motion "constellations over time," a sightline to the user's actual location, figure-of-the-night.
- **Phase 8:** Beauty + performance pass (Metal), accessibility, iPad. **Accessibility/iPad/localization are now spec'd** (see "Planned improvements" below); iPad deferred (no device to test).

## Planned improvements (spec'd from the 2026-06-28 full-app review)
A whole-codebase review produced concrete specs for the cross-cutting gaps. **These specs are
the source of truth for that work — read them before implementing, and keep them in sync.**
- **User preferences & Settings** → **`docs/preferences-spec.md`** *(approved to build first).*
  The app currently persists **no** scalar preferences (no `UserDefaults`/`AppStorage` anywhere):
  Sky filters/mode/FOV, Galaxy toggles, default house system/zodiac, the "Today" chart, units, and
  a home-location fallback all reset each launch. Spec proposes one injected `@Observable`
  `AppPreferences` (+ an App Group for widgets) and a Settings screen in `MoreMenu`.
- **Accessibility** → **`docs/accessibility-spec.md`** *(deferred).* No Dynamic Type, no VoiceOver
  representation of the Canvas/Metal surfaces (wheel/sphere/galaxy/sky), Reduce Motion honored in
  only one file.
- **Localization (EN + ES)** → **`docs/localization-spec.md`** *(deferred).* All copy hardcoded
  English; the interpretation corpus needs a localized content layer, not just a String Catalog.
- **Widgets / App Intents / Live Activities** → **`docs/widgets-spec.md`** *(planned).* Reuses
  existing engines (`VisibleSky`/`MoonPhase`/`AstroEvent`/`Daily`/`Forecast`); needs the
  preferences App Group first.
- **First-launch onboarding** → **`docs/onboarding-spec.md`** *(planned).* Replaces the cold
  empty-sky permission prompt with a short primed flow.
- **App icon & launch screen** *(ship blocker, not yet built).* There is **no asset catalog**
  (`*.xcassets`) and no custom `AppIcon`/launch screen — the app currently relies on
  `GENERATE_INFOPLIST_FILE`. Needs real icon artwork (a designer/image tool) plus the asset slots
  wired into `project.yml` before submission.
- **Backlog (ideas + technical/correctness notes)** → **`docs/improvement-backlog.md`**. Captures
  the **★ flagship features still unbuilt** (precession-gap "signs vs constellations" toggle,
  transit-to-sky, void-of-course Moon), per-surface feature ideas, and technical notes
  (`ContentView.refreshSky` on the main actor; god-object views `GalaxyMapView`/`ContentView`;
  no UI/snapshot test target).

## Open questions
- Final name (+ App Store availability check).
- ~~`SwiftAA` dependency or implement the math ourselves?~~ **Resolved:** rolled our own for astronomy; adding SwiftAA (MIT) for astrology planet positions (see `docs/astrology-spec.md`).
- ~~Astrology accuracy bar — and how to source ephemeris without Swiss Ephemeris licensing trouble.~~ **Resolved:** no Swiss Ephemeris (AGPL); own math on `CelestialCore` + SwiftAA. Validate ASC/MC/cusps against astro.com.
- AR via raw CoreMotion overlay vs. ARKit camera passthrough?
